# Local development API. Copy this file to .env and adjust if needed.
VITE_API_BASE_URL=http://localhost:4006
# Restaurants are addressed by path: /restaurant-slug. These two are optional
# fallbacks, used only if you later point a wildcard domain at this build.
# VITE_TENANT_ROOT_DOMAIN=restaurants.example.com
# VITE_DEFAULT_TENANT_SLUG=

# Production builds must supply an absolute, non-local HTTPS URL through the
# deployment environment, for example:
# VITE_API_BASE_URL=https://api.example.com

# Exact allowed editor origins, comma separated. No wildcard.
VITE_WEBSITE_WORKSPACE_ORIGINS=http://localhost:5173
